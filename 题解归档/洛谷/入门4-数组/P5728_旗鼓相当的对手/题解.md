# P5728 旗鼓相当的对手

> 题目链接：[洛谷 P5728](https://www.luogu.com.cn/problem/P5728)

## 元数据

| 项目 | 内容 |
|------|------|
| 难度 | 入门 |
| 标签 | 数组, 暴力 |
| 状态 | 已完成 |
| 语言 | C++ |

---

## 题目描述

n 个学生，每人三科成绩。若两人每科成绩差都不超过 5，且总分差不超过 10，则称为"旗鼓相当的对手"。求有多少对。

## 输入格式

第一行：n。
接下来 n 行，每行三个整数。

## 输出格式

一个整数，对手对数。

## 数据范围

$1 \le n \le 100$

## 样例

### 输入

```
3
56 89 90
78 89 67
56 89 90
```

### 输出

```
1
```

---

## 解题思路

### 审题过程

1. **理解题意**：n 个学生，每人三科成绩。两人"旗鼓相当"的条件：每科差≤5 且总分差≤10。
2. **关键观察**：需要同时满足 4 个条件。
3. **建模**：双循环枚举所有学生对，逐一检查条件。

### 方法一：暴力枚举

**核心**：`abs(scores[i][k] - scores[j][k]) <= 5` 对三科都要成立，且 `abs(sum1 - sum2) <= 10`。

## 代码实现

```cpp
#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    int scores[n][3];
    for (int i = 0; i < n; ++i) {
        cin >> scores[i][0] >> scores[i][1] >> scores[i][2];
    }

    int count = 0;
    for (int i = 0; i < n; ++i) {
        for (int j = i + 1; j < n; ++j) {
            int sum1 = scores[i][0] + scores[i][1] + scores[i][2];
            int sum2 = scores[j][0] + scores[j][1] + scores[j][2];

            if (abs(scores[i][0] - scores[j][0]) <= 5 &&
                abs(scores[i][1] - scores[j][1]) <= 5 &&
                abs(scores[i][2] - scores[j][2]) <= 5 &&
                abs(sum1 - sum2) <= 10) {
                count++;
            }
        }
    }

    cout << count << endl;
    return 0;
}
```

## 易错点

1. 四个条件要同时满足
2. j 从 i+1 开始避免重复计数
3. `abs()` 函数在 `<cmath>` 或 `<cstdlib>` 中

## 算法总结

| 项目 | 说明 |
|------|------|
| 算法类型 | 暴力枚举 |
| 时间复杂度 | $O(n^2)$ |
| 空间复杂度 | $O(n)$ |
| 核心技巧 | 双循环枚举 + 多条件判断 |

## 相关题目

1. P1614 爱与愁的心痛 — 区间和
2. P2550 彩票摇奖 — 多条件匹配

---

## 总结发散

### 核心模式：多条件判断

本题的关键是**同时满足多个条件**。在编程中，用 `&&` 连接所有条件。

### 映射到学习模块

| 本题用到的概念 | 学习模块中的位置 |
|----------------|------------------|
| 二维数组 | [学习层/01-C++/01-C++基础语法/07-静态数组与字符串.md](../../../../学习模块/02-学习层/01-C++/01-C++基础语法/07-静态数组与字符串.md) |
| 多条件判断 | [学习层/01-C++/01-C++基础语法/05-分支与循环控制结构.md](../../../../学习模块/02-学习层/01-C++/01-C++基础语法/05-分支与循环控制结构.md) — if 语句 |
| 暴力枚举 | [学习层/01-C++/03-基础算法体系/01-暴力枚举.md](../../../../学习模块/02-学习层/01-C++/03-基础算法体系/01-暴力枚举.md) |

---

## 相关学习

| 知识点 | 学习模块 | 本题应用 |
|--------|----------|----------|
| 二维数组 | [学习层/01-C++/01-C++基础语法/07-静态数组与字符串.md](../../../../学习模块/02-学习层/01-C++/01-C++基础语法/07-静态数组与字符串.md) | 存储每位同学各科成绩 |
| 数组遍历 | [学习层/01-C++/02-数据结构/01-链表原理与手写实现.md](../../../../学习模块/02-学习层/01-C++/02-数据结构/01-链表原理与手写实现.md) | 双循环枚举所有同学对 |

---

> 创建时间：2026-07-16 08:41
> 最后更新：2026-07-22
