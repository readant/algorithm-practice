# P1047 校门外的树

> 题目链接：[洛谷 P1047](https://www.luogu.com.cn/problem/P1047)

## 元数据

| 项目 | 内容 |
|------|------|
| 难度 | 入门 |
| 标签 | 数组, 模拟 |
| 状态 | 已完成 |
| 语言 | C++/C/Python/Java |

---

# P1047 [NOIP 2005 普及组] 校门外的树

## 题目描述

某校大门外长度为 $l$ 的马路上有一排树，每两棵相邻的树之间的间隔都是 $1$ 米。我们可以把马路看成一个数轴，马路的一端在数轴 $0$ 的位置，另一端在 $l$ 的位置；数轴上的每个整数点，即 $0,1,2,\dots,l$，都种有一棵树。


由于马路上有一些区域要用来建地铁。这些区域用它们在数轴上的起始点和终止点表示。已知任一区域的起始点和终止点的坐标都是整数，区域之间可能有重合的部分。现在要把这些区域中的树（包括区域端点处的两棵树）移走。你的任务是计算将这些树都移走后，马路上还有多少棵树。

## 输入格式

第一行有两个整数，分别表示马路的长度 $l$ 和区域的数目 $m$。

接下来 $m$ 行，每行两个整数 $u, v$，表示一个区域的起始点和终止点的坐标。

## 输出格式

输出一行一个整数，表示将这些树都移走后，马路上剩余的树木数量。

## 输入输出样例 #1

### 输入 #1

```
500 3
150 300
100 200
470 471

```

### 输出 #1

```
298
```

## 说明/提示

**【数据范围】**

- 对于 $20\%$ 的数据，保证区域之间没有重合的部分。
- 对于 $100\%$ 的数据，保证 $1 \leq l \leq 10^4$，$1 \leq m \leq 100$，$0 \leq u \leq v \leq l$。

**【题目来源】**

NOIP 2005 普及组第二题

---

## 解题思路

### 审题过程

1. **理解题意**：马路上每隔 1 米种一棵树，共 l+1 棵（从位置 0 到 l）。m 段区间被占用，这些区间内的树要移走。
2. **关键观察**：每个位置要么有树要么没有，这是一个二元状态问题。
3. **建模**：用布尔数组表示每个位置的状态——有树为 true，无树为 false。

### 方法一：布尔数组标记（推荐）

**第一步：初始化**
用 `tree[i]` 表示位置 i 是否有树。初始时所有位置都有树，全部设为 true。

**第二步：标记移除**
对每个占用区间 [u, v]，将 tree[u] 到 tree[v] 全部设为 false。

**第三步：统计结果**
遍历 0 到 l，统计 tree[i] 为 true 的个数。

```
初始：  T T T T T T T T T T T   (0~10，共11棵)
移除2~3：T T F F T T T T T T T
移除5~7：T T F F T F F F T T T
剩余：  1 1 0 0 1 0 0 0 1 1 1 = 6棵
```

**核心思路**：标记 + 统计。这是最直观的模拟方法。

## 代码实现

### C++

```cpp
#include <iostream>
using namespace std;

const int MAXN = 10010;
// 这里只会让第一个元素变成 true,其他的还是false
bool tree[MAXN] = {true};

int main() {
    int l, m;
    cin >> l >> m;
    // bool数组全局定义时默认为false,需要手动初始化
    for (int i = 0; i <= l; ++i)
        tree[i] = true;

    for (int i = 0; i < m; ++i) {
        int u, v;
        cin >> u >> v;
        for (int j = u; j <= v; ++j) {
            tree[j] = false;
        }
    }

    int cnt = 0;
    for (int i = 0; i <= l; ++i) {
        if (tree[i]) cnt++;
    }

    cout << cnt << '\n';
    return 0;
}
```

### Python

```python
MAXN = 10010
tree = [True] * MAXN

l, m = map(int, input().split())

for i in range(m):
    u, v = map(int, input().split())
    for j in range(u, v + 1):
        tree[j] = False

count = sum(1 for i in range(l + 1) if tree[i])
print(count)
```

### Java

```java
import java.util.Scanner;

public class Main {
    static final int MAXN = 10010;
    static boolean[] tree = new boolean[MAXN];

    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int l = sc.nextInt();
        int m = sc.nextInt();

        for (int i = 0; i <= l; i++)
            tree[i] = true;

        for (int i = 0; i < m; i++) {
            int u = sc.nextInt();
            int v = sc.nextInt();
            for (int j = u; j <= v; j++)
                tree[j] = false;
        }

        int count = 0;
        for (int i = 0; i <= l; i++) {
            if (tree[i]) count++;
        }

        System.out.println(count);
    }
}
```

### C

```c
#include <stdio.h>

#define MAXN 10010

int tree[MAXN];

int main() {
    int l, m;
    scanf("%d %d", &l, &m);

    for (int i = 0; i <= l; i++)
        tree[i] = 1;

    for (int i = 0; i < m; i++) {
        int u, v;
        scanf("%d %d", &u, &v);
        for (int j = u; j <= v; j++)
            tree[j] = 0;
    }

    int count = 0;
    for (int i = 0; i <= l; i++) {
        if (tree[i]) count++;
    }

    printf("%d\n", count);
    return 0;
}
```

## 易错点

1. 树的下标从 0 到 l，共 l+1 棵
2. 区间是闭区间 [u, v]
3. `bool` 数组全局定义时默认为 false，需要手动初始化

## 算法总结

| 项目 | 说明 |
|------|------|
| 算法类型 | 模拟, 标记数组 |
| 时间复杂度 | $O(l + m \times l)$ |
| 空间复杂度 | $O(l)$ |
| 核心技巧 | 布尔数组标记状态 |

## 相关题目

1. P1319 压缩技术 — 同样用布尔数组标记状态
2. P1161 开灯 — 灯的开关状态翻转
3. P5729 工艺品制作 — 二维版本的区间标记

---

## 总结发散

### 核心模式：标记数组

本题的核心是**用数组记录状态**。这个模式在很多题目中出现：

| 变体 | 题目 | 思路 |
|------|------|------|
| 一维标记 | P1047 校门外的树 | bool 数组标记位置 |
| 状态翻转 | P1161 开灯 | `light[idx] = !light[idx]` |
| 二维标记 | P5729 工艺品制作 | 二维数组标记区域 |
| 计数标记 | P2141 珠心算测验 | 标记两数之和 |

### 进阶方向

- **差分数组**：如果区间操作很多，可以用差分数组将 O(l) 的区间修改优化为 O(1)
- **线段树**：更高级的数据结构，支持区间修改和区间查询
- **容斥原理**：多个区间有重叠时，如何不重复计数

### 映射到学习模块

| 本题用到的概念 | 学习模块中的位置 |
|----------------|------------------|
| 布尔数组 | [学习层/01-C++/01-C++基础语法/07-静态数组与字符串.md](../../../../学习模块/02-学习层/01-C++/01-C++基础语法/07-静态数组与字符串.md) — 数组作为状态容器 |
| for 循环遍历 | [学习层/01-C++/01-C++基础语法/05-分支与循环控制结构.md](../../../../学习模块/02-学习层/01-C++/01-C++基础语法/05-分支与循环控制结构.md) — 循环结构 |
| 暴力模拟 | [学习层/01-C++/03-基础算法体系/01-暴力枚举.md](../../../../学习模块/02-学习层/01-C++/03-基础算法体系/01-暴力枚举.md) — 暴力法的思想起点 |

---

## 相关学习

| 知识点 | 学习模块 | 本题应用 |
|--------|----------|----------|
| 数组基础 | [学习层/01-C++/02-数据结构/01-链表原理与手写实现.md](../../../../学习模块/02-学习层/01-C++/02-数据结构/01-链表原理与手写实现.md) | 用数组标记每棵树是否存在 |
| 标记数组 | [学习层/01-C++/01-C++基础语法/07-静态数组与字符串.md](../../../../学习模块/02-学习层/01-C++/01-C++基础语法/07-静态数组与字符串.md) | `a[i] = 1` 标记位置 i 有树 |
| 循环结构 | [学习层/01-C++/01-C++基础语法/05-分支与循环控制结构.md](../../../../学习模块/02-学习层/01-C++/01-C++基础语法/05-分支与循环控制结构.md) | 遍历每个区间标记移除的树 |

---

> 创建时间：2026-07-16 08:41
> 最后更新：2026-07-22
