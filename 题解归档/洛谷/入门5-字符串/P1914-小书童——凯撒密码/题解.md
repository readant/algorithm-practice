# P1914 小书童——凯撒密码

## 题目背景

某蒟蒻迷上了 "小书童"，有一天登陆时忘记密码了（他没绑定邮箱 or 手机），于是便把问题抛给了神犇你。

## 题目描述

蒟蒻虽然忘记密码，但他还记得密码是由一个字符串组成。密码是由原文字符串（由不超过 50 个小写字母组成）中每个字母向后移动 $n$ 位形成的。`z` 的下一个字母是 `a`，如此循环。他现在找到了移动前的原文字符串及 $n$，请你求出密码。

## 输入格式

第一行：$n$。第二行：未移动前的一串字母。

## 输出格式

一行，是此蒟蒻的密码。

## 输入输出样例 #1

### 输入 #1

```
1
qwe
```

### 输出 #1

```
rxf
```

## 说明/提示

字符串长度 $\le 50$，$1 \leq n \leq 26$。

## 解题思路

核心公式：`(c - 'a' + n) % 26 + 'a'`
1. `c - 'a'`：将字符映射到0-25
2. `+ n`：偏移n位
3. `% 26`：循环处理（z后回到a）
4. `+ 'a'`：转回ASCII字符

## C++ 解决方案

```cpp
#include <iostream>
#include <string>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n;
    string s;
    cin >> n >> s;
    
    for (char &c : s) {
        c = (c - 'a' + n) % 26 + 'a';
    }
    
    cout << s;
    return 0;
}
```

## Python 解决方案

```python
n = int(input())
s = input()
result = ""
for c in s:
    result += chr((ord(c) - ord('a') + n) % 26 + ord('a'))
print(result)
```

## C 解决方案

```c
#include <stdio.h>
#include <string.h>

int main() {
    int n;
    char s[51];
    scanf("%d %s", &n, s);
    
    for (int i = 0; i < strlen(s); i++) {
        s[i] = (s[i] - 'a' + n) % 26 + 'a';
    }
    
    printf("%s", s);
    return 0;
}
```

## Java 解决方案

```java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        String s = sc.next();
        
        StringBuilder sb = new StringBuilder();
        for (char c : s.toCharArray()) {
            sb.append((char) ((c - 'a' + n) % 26 + 'a'));
        }
        
        System.out.print(sb);
    }
}
```

---

## 相关学习

| 知识点 | 学习模块 | 本题应用 |
|--------|----------|----------|
| 字符串处理 | [学习层/02-核心语法/04-数组与字符串.md](../../../../学习模块/02-学习层/01-C++/02-核心语法/04-数组与字符串.md) | 遍历字符串逐字符加密 |
| 字符运算 | [参考层/01-C++/01-STL容器.md](../../../../学习模块/01-参考层/01-C++/01-STL容器.md) | `c - 'a'` 获取字符偏移量 |
| 取模运算 | [学习层/02-核心语法/01-变量与基本数据类型.md](../../../../学习模块/02-学习层/01-C++/02-核心语法/01-变量与基本数据类型.md) | `(c - 'a' + n) % 26` 循环移位 |

---

## 总结发散

### 核心模式：字符偏移 + 取模

凯撒密码的核心公式：`(c - 'a' + n) % 26 + 'a'`。这个模式在很多字符处理题中出现：

| 应用 | 公式 |
|------|------|
| 字符偏移 | `(c - 'a' + n) % 26 + 'a'` |
| 大小写转换 | `c ^ 32`（ASCII 差值） |
| 字符统计 | `cnt[c - 'a']++` |

### 映射到学习模块

| 本题用到的概念 | 学习模块中的位置 |
|----------------|------------------|
| 字符运算 | [学习层/01-C++/02-核心语法/04-数组与字符串.md](../../../../学习模块/02-学习层/01-C++/02-核心语法/04-数组与字符串.md) |
| 取模运算 | [学习层/01-C++/02-核心语法/01-变量与基本数据类型.md](../../../../学习模块/02-学习层/01-C++/02-核心语法/01-变量与基本数据类型.md) |
